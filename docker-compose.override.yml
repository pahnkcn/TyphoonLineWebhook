# Development-only services (not for production)
# This file is automatically loaded by docker-compose alongside docker-compose.yml
version: '3'
services:
  adminer:
    image: adminer
    restart: always
    ports:
      - "8080:8080"
    environment:
      ADMINER_DEFAULT_SERVER: db
    depends_on:
      - db
